# 密钥系统缺口调研

日期：2026-06-11

## 1. 当前草案已覆盖的内容

你当前列出的内容已经覆盖了密钥系统的基础骨架：

- 密钥类型：离线密钥、在线密钥
- 密钥基础信息：合同标号、客户公司信息、过期时间、系统类型
- 角色：超级管理员、管理员、普通员
- 核心管理动作：人员增删改查、密钥增删改查、展示系统类型增删改查
- 基础查看范围：超级管理员看全局，管理员看团队，普通员看自己

但这更像“后台 CRUD 权限草图”，还不是一个完整的授权/License 密钥系统。缺口主要集中在密钥生命周期、授权模型、安全审计、在线/离线校验、组织边界和运营治理。

## 2. 高优先级缺口

### P0：必须先补齐，否则系统容易失控

| 缺口 | 当前问题 | 建议补齐 |
|---|---|---|
| 密钥生命周期状态 | 只有“过期时间”，没有密钥从创建到失效的完整状态 | 增加状态：草稿、待审批、已签发、已激活、暂停、吊销、已过期、已续期、已作废 |
| 密钥生成规则 | 未说明密钥如何生成、是否可预测、是否防伪 | 明确密钥算法、签名机制、随机性、密钥长度、版本号、校验位，避免可枚举或伪造 |
| 密钥存储安全 | 未说明后台是否保存明文密钥 | 后台建议只存哈希、签名元数据和部分脱敏展示；明文只在签发时短暂显示/下载 |
| 在线密钥校验机制 | 只有“在线密钥”概念，没有校验流程 | 定义激活、心跳、校验频率、失败重试、宽限期、离线容错、吊销同步 |
| 离线密钥校验机制 | 只有“离线密钥”概念，没有离线授权边界 | 定义离线授权文件/签名包、机器码绑定、有效期、导入导出、离线续期和吊销难题 |
| 吊销与停用 | 未说明客户违约、泄露、合同终止后如何处理 | 增加吊销、暂停、恢复、黑名单、吊销原因、吊销生效范围 |
| 审计日志 | 当前没有操作留痕设计 | 记录创建、修改、查看、下载、激活、续期、吊销、删除等关键动作，含操作者、时间、IP、对象、变更前后 |
| 权限边界 | 角色写了，但数据范围和敏感动作不清楚 | 增加 RBAC + 数据域：公司、团队、客户、合同、系统类型；敏感操作需审批或二次确认 |
| 过期提醒 | 只展示过期时间，不保证有人处理 | 增加 90/60/30/7 天到期提醒、已过期告警、续期待办 |
| “新节点”定义不清 | 草案中出现两次“新节点”，但没有语义 | 明确是组织节点、设备节点、展示系统节点，还是授权部署节点 |

### P1：影响可运营、可维护、可扩展

| 缺口 | 当前问题 | 建议补齐 |
|---|---|---|
| 客户/合同模型 | “客户公司信息”和“合同标号”过于粗 | 建议拆成客户、合同、项目、授权单、密钥，多表关联 |
| 授权权益模型 | 密钥只绑定系统类型，不知道授权了什么能力 | 增加授权范围：系统类型、模块、版本、设备数、账号数、并发数、数据采集点数、有效区域 |
| 设备/机器绑定 | 没说明密钥是否限制设备 | 增加设备指纹、机器码、激活设备列表、换机申请、解绑审批 |
| 续期/升级/变更 | 只有增删改查，没有业务流程 | 增加续期、扩容、降级、系统类型变更、合同变更、客户迁移 |
| 审批流 | 管理员可直接增删改密钥，风险高 | 高风险动作增加审批：生成正式密钥、延长有效期、吊销、导出、删除 |
| 删除策略 | 密钥系统不宜物理删除关键数据 | 改为软删除/作废，保留审计和历史版本 |
| 版本管理 | 密钥修改后历史不可追溯 | 增加密钥版本、变更记录、历史授权快照 |
| 查询与报表 | 只有“查看”，没有运营分析 | 增加按客户、合同、过期时间、系统类型、状态、负责人筛选导出 |
| 异常检测 | 没有滥用识别 | 增加同一密钥多地激活、频繁校验失败、异常 IP、超设备数告警 |
| API 与集成 | 没有说明与展示系统如何对接 | 提供密钥验证 API、离线验证 SDK、回调或 Webhook、接口鉴权和限流 |

### P2：提升体验和管理效率

| 缺口 | 当前问题 | 建议补齐 |
|---|---|---|
| 批量操作 | 密钥多时 CRUD 低效 | 批量导入、批量续期、批量导出、批量分配负责人 |
| 模板化签发 | 每次手动填写容易出错 | 按系统类型/客户类型配置授权模板 |
| 客户自助入口 | 所有事情都靠内部人员处理 | 可选客户门户：查看授权、下载离线包、提交换机/续期申请 |
| 通知渠道 | 只有后台查看，容易漏 | 企业微信/飞书/邮件/短信通知 |
| 数据字典 | 系统类型、密钥类型、状态、客户行业等缺统一字典 | 增加可配置字典和禁用策略 |

## 3. 按角色补齐权限设计

### 超级管理员

现有权限过大，需要补充“边界”和“审计”：

- 可管理全局人员、团队、客户、合同、系统类型、授权模板
- 可查看全局密钥，但敏感字段默认脱敏
- 可执行吊销、恢复、作废、强制解绑等高危操作
- 高危操作需要二次确认，并记录不可篡改审计日志
- 可配置角色权限、审批流、系统参数、通知策略

### 管理员

需要明确管理员是“团队管理员”还是“客户管理员”：

- 只能管理自己团队/负责客户范围内的人员和密钥
- 可创建密钥申请，但正式签发可要求超级管理员审批
- 可查看团队成员创建/负责的密钥
- 可处理续期、换机、解绑申请
- 不应默认拥有删除/吊销/延长多年有效期等高危权限

### 普通员

当前“增删改查密钥”权限偏大：

- 建议改为：创建密钥申请、查看自己负责密钥、编辑草稿、提交审批
- 不建议允许普通员直接删除、吊销、延长有效期、导出完整密钥
- 可查看自己客户/合同下的授权状态和到期提醒

## 4. 密钥生命周期建议

建议定义一条标准流程：

1. 创建授权申请：选择客户、合同、系统类型、授权范围、有效期、密钥类型。
2. 审批：根据金额、有效期、系统类型、授权范围决定是否需要审批。
3. 签发：系统生成密钥或离线授权文件，记录签发人和签发时间。
4. 激活：在线密钥通过服务端激活；离线密钥通过授权文件导入。
5. 使用校验：展示系统按规则校验授权状态、功能范围和到期时间。
6. 监控：记录激活设备、校验次数、异常失败、即将过期。
7. 变更：支持续期、扩容、换机、暂停、恢复。
8. 失效：过期、吊销、作废或合同终止。
9. 归档：保留历史授权和审计记录。

## 5. 在线密钥需要单独设计的点

- 激活流程：密钥输入后，服务端校验合同、客户、状态、有效期、设备限制。
- 心跳策略：例如每天/每周校验一次，不建议每次启动都强依赖网络。
- 宽限期：网络不可用时允许继续使用 N 天，超过后限制核心功能。
- 吊销同步：密钥被吊销后，下次心跳应生效。
- 设备数量控制：限制激活设备数，支持解绑和换机。
- 接口安全：验证 API 需要鉴权、签名、防重放、限流。
- 异常识别：同一密钥在多个地区/设备快速激活时告警。

## 6. 离线密钥需要单独设计的点

- 授权载体：建议使用签名授权文件或签名字符串，不要只靠可读文本。
- 机器绑定：可绑定机器码、主板/硬盘/网卡摘要，但要考虑硬件变更容错。
- 离线续期：支持生成新的授权文件并导入覆盖。
- 离线吊销难题：纯离线环境无法实时吊销，需要通过短有效期、人工回收、下次升级校验来降低风险。
- 防篡改：客户端只验证签名，不应内置可用于签发的私钥。
- 时间回拨：需要处理用户修改系统时间绕过过期的问题，例如记录最近可信时间。

## 7. 数据模型建议

最少建议拆成以下对象：

- 用户：账号、角色、团队、状态、MFA 状态
- 团队/组织：上级组织、负责人、数据范围
- 客户公司：公司名称、统一社会信用代码、联系人、行业、状态
- 合同：合同编号、客户、金额、开始时间、结束时间、合同文件、销售/负责人
- 展示系统类型：类型名称、版本、模块、状态
- 授权模板：系统类型、默认有效期、默认模块、默认设备数
- 密钥/授权：密钥编号、密钥类型、状态、客户、合同、授权范围、有效期、负责人
- 激活记录：设备指纹、设备名称、IP、激活时间、最近校验时间、状态
- 变更记录：变更人、变更字段、变更前、变更后、原因
- 审计日志：所有敏感操作和访问记录
- 通知记录：到期提醒、吊销通知、异常告警

## 8. Assumption Mapping

| 风险区 | 隐含假设 | 可能出错 | 信心 | 验证方式 |
|---|---|---|---|---|
| Value | 用户只需要管理密钥基础信息 | 实际还需要续期、换机、吊销、到期提醒等流程 | 高 | 访谈销售、交付、售后，收集最近 20 个授权处理案例 |
| Value | 合同标号足够关联业务 | 一个合同可能对应多个项目、多个系统、多个授权批次 | 高 | 梳理历史合同和交付清单 |
| Usability | 三类角色足够 | 实际可能有销售、实施、售后、财务、审计等角色 | 中 | 做权限矩阵工作坊 |
| Usability | 管理员可以直接 CRUD 密钥 | 高危操作太多，误操作难以追回 | 高 | 设计原型并做权限走查 |
| Viability | 密钥删除是正常需求 | 删除会破坏审计、合同追责和客户争议处理 | 高 | 与法务/财务/售后确认留痕要求 |
| Viability | 离线密钥能像在线密钥一样管理 | 离线环境无法实时吊销和监控 | 高 | 明确离线客户场景和风险接受标准 |
| Feasibility | 展示系统容易接入验证 | 不同系统版本、离线环境、旧客户端可能难以统一 | 中 | 做一个验证 SDK/API 技术 Spike |
| Feasibility | 机器码绑定稳定 | 硬件变更、虚拟机、系统重装会造成误封 | 中 | 测试目标客户常见设备环境 |

## 9. Pre-Mortem：假设 14 天后上线失败

### Tigers：真实风险

| 风险 | 紧急程度 | 为什么严重 | 缓解动作 |
|---|---|---|---|
| 普通员可直接增删改查密钥 | Launch-Blocking | 容易误发、越权、删除关键授权记录 | 收窄普通员权限，引入审批和软删除 |
| 没有审计日志 | Launch-Blocking | 出现客户争议、泄露、误操作时无法追责 | 上线前必须记录关键操作日志 |
| 没有吊销/暂停机制 | Launch-Blocking | 合同终止或密钥泄露后无法止损 | 增加状态机和吊销同步逻辑 |
| 离线密钥方案不清 | Launch-Blocking | 离线授权一旦发出，可能长期不可控 | 定义离线授权文件、短有效期、机器绑定和续期流程 |
| 在线校验依赖不清 | Fast-Follow | 网络不稳定时客户系统可能不可用 | 设计心跳、缓存和宽限期 |
| 过期提醒缺失 | Fast-Follow | 到期后客户突然不可用，售后压力大 | 增加多级提醒和续期流程 |
| 系统类型只是 CRUD | Fast-Follow | 授权范围无法精细控制模块/版本/设备数 | 增加授权权益模型 |

### Paper Tigers：看起来重要但不应过度投入

| 担忧 | 判断 |
|---|---|
| 一开始就做完整客户自助门户 | 可后置。内部授权流程先跑通更重要 |
| 一开始就支持复杂多级组织 | 可从团队/客户负责人两级开始，避免过早复杂化 |
| 一开始就做很复杂的数据分析大屏 | 可后置。先保证查询、筛选、导出、提醒 |

### Elephants：团队可能还没讨论清楚

| 未讨论清楚的问题 | 建议调查 |
|---|---|
| “密钥”到底是 License 授权码，还是密码学意义上的 secret/key | 先统一术语，否则安全设计会跑偏 |
| “新节点”指组织节点、设备节点、系统节点还是部署节点 | 把节点定义成具体业务对象 |
| 客户是否存在完全离线环境 | 决定离线授权、吊销和续期策略 |
| 展示系统有多少版本需要兼容 | 决定 SDK/API 兼容策略 |
| 谁对合同和授权一致性负责 | 明确销售、交付、管理员、超级管理员的责任边界 |

## 10. 建议的 MVP 范围

第一版不要只做 CRUD，建议至少包含：

- 用户、角色、团队、客户、合同、系统类型基础管理
- 密钥申请、审批、签发、激活、续期、吊销、作废
- 在线密钥验证 API
- 离线授权文件生成与导入验证机制
- 授权范围：系统类型、模块、版本、设备数、有效期
- 设备激活记录
- 审计日志
- 到期提醒和异常告警
- 查询、筛选、导出
- 权限矩阵和数据范围控制

## 11. 参考基准

- [OWASP Key Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Key_Management_Cheat_Sheet.html)：强调密钥生命周期、生成、分发、销毁、泄露恢复、存储等管理要求。
- [OWASP Secrets Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html)：强调吊销、轮换、访问控制和审计。
- [OWASP REST Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html)：指出 API Key 容易被泄露，不能单独依赖 API Key 保护高价值资源，并应支持限流和吊销。
- [NIST SP 800-57 Part 1 Rev. 5](https://csrc.nist.gov/pubs/sp/800/57/pt1/r5/final)：提供密钥管理生命周期、授权、备份、恢复、泄露、密钥库存等通用建议。
