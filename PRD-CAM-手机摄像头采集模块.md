# CAM 手机摄像头采集模块产品需求文档

## 1. Summary

CAM 是一个“手机摄像头采集模块”，目标是让电脑可以直接调用手机摄像头，把手机画面作为电脑端的视频采集输入。

本项目不是面向公开售卖的商业化摄像头产品。它更像一个基础能力模块，可用于演示、采集、识别、会议、算法输入、数据记录或其他上层系统。

## 2. Contacts

| 角色 | 负责人 | 说明 |
|---|---|---|
| 产品负责人 | 待定 | 定义采集场景、功能范围和验收标准 |
| 客户端开发 | 待定 | 负责手机端摄像头采集和电脑端接收 |
| 桌面端开发 | 待定 | 负责电脑端预览、设备输出和配置 |
| 测试负责人 | 待定 | 负责连接稳定性、画质、延迟和兼容性测试 |
| 使用方 | 待定 | 提供真实采集场景和设备条件 |

## 3. Background

很多电脑自带摄像头画质差、角度固定、移动不方便。手机摄像头画质更高，也更容易调整角度和距离。

本模块要解决的问题是：在不购买额外摄像头的情况下，让电脑可以把手机当作一个可用的视频采集源。

当前系统和市场已经有类似能力，例如 Windows 11 可以调用部分 Android 手机摄像头，macOS 可以通过连续互通相机调用 iPhone，Android 14 也支持部分 USB 摄像头模式。但这些能力有系统限制，且不一定满足自定义采集、算法输入、内部系统集成和统一控制的需求。

因此，本项目的重点不是做一个“大而全”的摄像头 App，而是做一个稳定、可控、可接入其他系统的采集模块。

## 4. Objective

### 4.1 目标

让用户可以在电脑端快速连接手机摄像头，并稳定获得实时视频画面。

第一阶段重点：

- 手机端采集摄像头画面。
- 电脑端接收并显示实时画面。
- 支持基础参数配置。
- 保证连接稳定、延迟可接受。
- 为后续算法、录制、会议或系统集成预留接口。

### 4.2 为什么重要

该能力可以降低采集设备成本，也能提升采集灵活性。用户只需要一台手机和一台电脑，就可以完成摄像头采集。

对于内部项目来说，它可以作为通用输入源，服务后续功能：

- 视频预览。
- 图像识别。
- 姿态采集。
- 远程查看。
- 录制留档。
- 多角度采集。
- 会议或直播输入。

### 4.3 Key Results

| 指标 | 目标 |
|---|---|
| 首次连接成功率 | 常见设备组合下达到 90% 以上 |
| 连接耗时 | 同一局域网内首次连接不超过 15 秒 |
| 视频延迟 | Wi-Fi 模式下目标低于 300ms，优先保证流畅 |
| 基础画质 | 支持 720p，优先支持 1080p |
| 稳定性 | 连续运行 30 分钟无明显卡死或崩溃 |
| 断线恢复 | 网络断开后重新连接不需要重启整个程序 |
| 兼容性 | 第一版至少支持 Android 手机 + Windows 电脑 |

## 5. Market Segment(s)

这里的“市场”不是商业销售市场，而是使用场景。

### 5.1 主要使用人群

| 使用人群 | 需求 |
|---|---|
| 内部研发人员 | 需要一个稳定的视频输入源，用于算法、识别或系统调试 |
| 测试人员 | 需要反复连接、采集、录制和验证画面 |
| 演示人员 | 需要快速把手机画面接入电脑进行展示 |
| 数据采集团队 | 需要用低成本设备完成视频采集 |
| 业务系统开发者 | 需要把手机摄像头接入现有桌面软件或 Web 系统 |

### 5.2 主要场景

- 手机对准人体、物体、设备或场地，电脑实时显示画面。
- 电脑端系统读取实时画面，用于算法处理。
- 手机作为外置摄像头，用于会议、直播或演示。
- 多台手机分别作为不同角度的摄像头。
- 采集过程需要录制视频或保存关键帧。

### 5.3 约束

- 不优先考虑商业化账号、订阅、支付和用户增长。
- 第一版不追求全平台覆盖。
- 第一版不追求复杂美颜、滤镜、虚拟背景。
- 第一版不优先支持公网远程连接。
- 第一版优先保证本地局域网内稳定使用。

## 6. Value Proposition(s)

### 6.1 用户任务

用户想用手机摄像头替代电脑摄像头，并在电脑上获得稳定实时画面。

### 6.2 用户收益

- 不需要额外采购 USB 摄像头。
- 手机摄像头画质通常更好。
- 手机可以灵活摆放，采集角度更自由。
- 电脑端可以统一预览、控制、录制和接入其他系统。
- 后续可以扩展成算法输入源。

### 6.3 避免的痛点

- 电脑摄像头画质差。
- 外接摄像头线缆限制大。
- 不同软件调用摄像头方式不统一。
- 系统原生方案能力有限，难以自定义。
- 采集画面无法稳定进入业务系统。

### 6.4 与已有方案的差异

| 方案 | 差异点 |
|---|---|
| Windows/Apple 原生能力 | 原生方案适合会议，但不一定方便做内部采集和系统集成 |
| DroidCam/iVCam 等工具 | 偏通用工具，内部系统可控性和二次开发能力有限 |
| OBS | 功能强，但链路较重，不适合作为轻量采集模块 |
| 本 CAM 模块 | 聚焦稳定采集、可控参数、可集成接口和内部使用 |

## 7. Solution

## 7.1 用户流程

### 基础连接流程

1. 用户在电脑端打开 CAM 接收程序。
2. 用户在手机端打开 CAM 采集程序。
3. 手机端自动发现同一局域网内的电脑端，或用户扫码连接。
4. 连接成功后，电脑端显示实时预览画面。
5. 用户可以切换前后摄像头、调整分辨率、开始或停止采集。
6. 电脑端可以将画面输出给上层系统、录制文件或虚拟摄像头。

### 异常流程

| 异常 | 处理方式 |
|---|---|
| 手机和电脑不在同一网络 | 提示检查网络，并提供手动输入 IP 的方式 |
| 手机断网 | 电脑端保留最后状态，并显示正在重连 |
| 手机锁屏 | 手机端提示保持前台运行，必要时启用防锁屏 |
| 摄像头权限未授权 | 手机端提示授权摄像头权限 |
| 电脑端端口被占用 | 自动切换端口或提示用户修改端口 |

## 7.2 Key Features

### P0 必须有

| 功能 | 说明 | 验收标准 |
|---|---|---|
| 手机摄像头采集 | 手机端调用摄像头并采集实时画面 | 能显示手机摄像头画面 |
| 局域网连接 | 手机和电脑在同一 Wi-Fi 下连接 | 电脑端能发现或连接手机 |
| 电脑端实时预览 | 电脑端显示手机画面 | 画面连续、不卡死 |
| 前后摄像头切换 | 手机端或电脑端切换摄像头 | 切换后画面正常恢复 |
| 分辨率选择 | 支持至少 720p，优先支持 1080p | 修改后画面尺寸生效 |
| 开始/停止采集 | 用户可控制采集状态 | 停止后不再传输视频流 |
| 断线重连 | 网络波动后可恢复 | 不需要重启程序即可重新连接 |
| 连接状态提示 | 显示未连接、连接中、采集中、重连中 | 状态与实际连接一致 |

### P1 应该有

| 功能 | 说明 |
|---|---|
| USB 连接 | 提供比 Wi-Fi 更稳定的连接方式 |
| 电脑端录制 | 将采集画面保存为本地视频文件 |
| 截图 | 保存当前帧图片 |
| 横竖屏适配 | 自动识别手机方向并正确显示 |
| 镜像开关 | 支持左右镜像 |
| 帧率选择 | 支持 15fps、30fps，可根据设备能力调整 |
| 画质参数 | 支持曝光、亮度、对焦等基础控制 |
| 虚拟摄像头输出 | 在 Zoom、Teams、OBS、浏览器等软件中作为摄像头出现 |

### P2 可以以后做

| 功能 | 说明 |
|---|---|
| 多手机接入 | 一台电脑接入多台手机 |
| 多机位切换 | 电脑端切换不同手机画面 |
| 远程公网连接 | 不在同一局域网时也可连接 |
| 云端中继 | 通过服务器转发视频流 |
| AI 画面处理 | 人像分割、背景虚化、目标检测 |
| SDK/API | 让其他系统通过接口读取画面 |
| Web 控制台 | 浏览器中查看和管理采集状态 |

## 7.3 页面与交互

### 手机端

手机端应保持简单，只负责采集和连接。

主要页面：

- 权限授权页。
- 摄像头预览页。
- 连接状态页。
- 设备选择页。
- 设置页。

主要控件：

- 开始采集。
- 停止采集。
- 切换摄像头。
- 切换横竖屏。
- 选择分辨率。
- 查看电脑连接状态。

### 电脑端

电脑端负责接收、预览、配置和输出。

主要区域：

- 实时预览区。
- 已连接设备列表。
- 分辨率和帧率设置。
- 开始/停止按钮。
- 录制/截图按钮。
- 连接状态和错误提示。
- 输出方式设置。

## 7.4 Technology

### 推荐技术路线

第一版建议采用“手机端采集 + 局域网传输 + 电脑端接收预览”的方式。

```text
手机端 App
  摄像头采集
  编码视频流
  通过 Wi-Fi 发送

电脑端 App
  发现手机设备
  接收视频流
  解码并预览
  输出给业务系统或虚拟摄像头
```

### 手机端

| 平台 | 建议 |
|---|---|
| Android | 第一版优先支持。可使用 CameraX 或 Camera2 |
| iOS | 第二阶段支持。可使用 AVFoundation |

### 电脑端

| 平台 | 建议 |
|---|---|
| Windows | 第一版优先支持 |
| macOS | 第二阶段支持 |
| Linux | 暂不优先 |

### 传输方式

| 方式 | 优点 | 缺点 | 阶段 |
|---|---|---|---|
| Wi-Fi 局域网 | 开发快、无数据线、体验简单 | 受网络质量影响 | P0 |
| USB | 稳定、延迟低 | 开发和适配更复杂 | P1 |
| WebRTC | 适合低延迟实时传输 | 实现复杂度较高 | P1 |
| RTSP/HTTP MJPEG | 实现简单、调试方便 | 延迟和效率可能较差 | 可用于原型 |

### 视频输出方式

| 输出方式 | 说明 | 优先级 |
|---|---|---|
| 电脑端预览窗口 | 最基础能力 | P0 |
| 本地视频文件 | 用于采集留档 | P1 |
| 图片帧接口 | 用于算法读取 | P1 |
| 虚拟摄像头 | 让其他软件识别为摄像头 | P1/P2 |
| SDK/API | 给业务系统集成 | P2 |

## 7.5 数据与配置

### 需要保存的配置

- 上次连接的手机设备。
- 默认分辨率。
- 默认帧率。
- 默认摄像头方向。
- 默认输出方式。
- 录制文件保存路径。

### 不应该保存的内容

- 默认不保存视频内容。
- 默认不上传视频内容。
- 默认不采集用户隐私数据。
- 除非用户主动录制，否则不落盘视频。

## 7.6 安全与隐私

- 视频流默认只在本地局域网内传输。
- 手机端必须明确显示正在采集。
- 电脑端必须明确显示已连接设备。
- 用户可以随时停止采集。
- 不默认上传云端。
- 不默认后台静默打开摄像头。
- 若后续支持公网连接，需要加入鉴权、加密和连接确认。

## 7.7 Assumptions

| 假设 | 验证方式 |
|---|---|
| 第一批用户主要使用 Android + Windows | 统计目标设备环境 |
| Wi-Fi 延迟可以满足基础采集 | 做局域网延迟测试 |
| 720p 已能满足第一版需求 | 用真实场景对比 720p/1080p |
| 用户更重视稳定性而不是滤镜效果 | 访谈使用方，观察实际使用 |
| 电脑端预览比虚拟摄像头更适合作为第一版 | 用原型验证接入场景 |

## 8. Release

## 8.1 MVP 范围

MVP 目标是验证“手机摄像头画面可以稳定进入电脑端”。

包含：

- Android 手机端采集。
- Windows 电脑端接收。
- Wi-Fi 局域网连接。
- 实时预览。
- 前后摄像头切换。
- 720p/1080p 选择。
- 开始/停止采集。
- 断线重连。
- 基础错误提示。

不包含：

- iOS 支持。
- macOS 支持。
- 公网远程连接。
- 多手机接入。
- 复杂美颜和滤镜。
- 云端账号系统。
- 支付和商业化能力。

## 8.2 版本拆分

### V0.1 原型验证

周期：1 到 2 周。

目标：

- 打通手机端采集到电脑端预览。
- 可以在同一局域网内连接。
- 能连续运行 10 分钟。

### V0.2 可用版本

周期：2 到 4 周。

目标：

- 增加连接状态。
- 增加断线重连。
- 增加分辨率选择。
- 增加前后摄像头切换。
- 连续运行 30 分钟。

### V0.3 集成版本

周期：3 到 6 周。

目标：

- 提供录制或截图。
- 提供图片帧接口或视频流接口。
- 支持业务系统读取画面。
- 完成真实场景测试。

### V1.0 稳定版本

周期：6 到 10 周。

目标：

- 支持稳定安装和使用。
- 支持主流 Android 设备。
- 支持基础日志和问题定位。
- 提供使用说明和测试报告。

## 8.3 验收标准

| 模块 | 验收标准 |
|---|---|
| 手机端采集 | 能正常打开摄像头并显示预览 |
| 连接 | 手机和电脑可在同一局域网内连接 |
| 预览 | 电脑端画面连续显示，无明显卡死 |
| 延迟 | 普通 Wi-Fi 环境下低于 300ms 或达到可接受观感 |
| 画质 | 至少支持 720p，优先支持 1080p |
| 稳定性 | 连续运行 30 分钟不崩溃 |
| 重连 | 手机断网后恢复网络可重新连接 |
| 权限 | 摄像头权限被拒绝时有明确提示 |
| 隐私 | 未点击开始采集时不传输画面 |

## 8.4 风险

| 风险 | 影响 | 应对 |
|---|---|---|
| 不同 Android 机型摄像头能力差异大 | 画质、帧率、对焦表现不一致 | 先限定测试机型，再扩大兼容范围 |
| Wi-Fi 环境不稳定 | 延迟和卡顿 | 提供码率/分辨率降级，后续支持 USB |
| 虚拟摄像头实现复杂 | 影响会议软件调用 | 第一版先做电脑端预览，后续再做虚拟摄像头 |
| 手机后台限制 | 锁屏或切后台后采集中断 | 手机端保持前台运行，并提示用户 |
| 权限和安全限制 | 摄像头无法打开或被系统拦截 | 明确权限申请和错误提示 |

## 8.5 后续开放问题

- 第一版是否必须被 Zoom、腾讯会议、飞书会议识别为摄像头？
- 第一版是否只做内部系统预览，而不做虚拟摄像头？
- 是否必须支持 iPhone？
- 采集画面是否需要保存？
- 是否需要把视频流接入现有算法模块？
- 是否需要多手机同时采集？
- 是否要求离线环境可用？

